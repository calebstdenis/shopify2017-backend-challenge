let http = require('http');
let assert = require('assert');

describe("Shopify Paginated API Handler", () => {
    describe("parseApiPage", () => {
        it("should make a GET call to the given endpoint and return a promise of the parsed JSON data", () => {
            
        });
    })
});